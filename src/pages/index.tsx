import { Layout } from '@/components/Layout';
import { NFTCard } from '@/components/NFTCard';

const Index = () => {
  return (
    <Layout>
      <NFTCard />
    </Layout>
  );
};

export default Index;
